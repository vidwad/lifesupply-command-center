# LifeSupply Website Development — Stage Status

Prepared September 8, 2026. This tracker records the website workstream separately from Command Center Phase 11. The instruction package itself does not complete a website stage or accept a release gate.

## Current position

- Instruction baseline reviewed: main commit `148617e60a54d11c79786a66f0a75a1ed70f54cb`.
- Instruction package: PR #67 (`docs/lifesupply-website-staged-development`), squash-merged to `main` as `a355cf6`.
- Stage 1: PR #68 (`claude/website-stage-01-baseline`), squash-merged as `fac59a4`. Documentation and evidence only.
- Stage 2: PR #69 (`claude/website-stage-02-shell-home-about`), squash-merged as `19ca3d5`.
- Stage 3: built stacked on the Stage 2 branch as PR #70. GitHub closed #70 automatically when its base branch was deleted after #69 merged, so the identical commits were rebased onto `main` and merged as PR #71, squash `fab88e7`. Merge order #67 → #68 → #69 → Stage 3 was preserved.
- Merges were performed on the product owner's instruction of 2026-09-08 to commit and squash-merge after each stage. Each merge triggered the existing automatic Vercel production build of the public alias (D-11), which is `noindex` and has no custom domain; `lifesupplyhealth.com` still serves the legacy WordPress site.
- Stage 4: PR #72 (`claude/website-stage-04-metabolic-health`), squash-merged as `622c7a8`.
- Stage 5: PR #73 (`claude/website-stage-05-partners-investors`), squash-merged as `2cd81c3`.
- Stage 6: PR #74 (`claude/website-stage-06-governed-publishing`), squash-merged as `de906d7`; verification record PR #75 (`c065d3d`). No migration included; the additive Stage 6 migration is prepared under `docs/website-development/migrations/` and waits for WEB-10.
- Stage 7: PR #76 (`claude/website-stage-07-inquiry-capture`), squash-merged as `0743a56`; verification record PR #77 (`ed15745`). Path (b): adapter-backed intake, prepared migration, form unpublished.
- Stage 8: PR #78 (`claude/website-stage-08-operating-site-integration`), squash-merged as `68d6af5`; verification record PR #79 (`1e04402`). External rows all `proposed`; measurement inert; projection contract only.
- Stage 9 executed on branch `claude/website-stage-09-release-verification` from `main` at `1e04402`. Not stacked. Legacy URL map, redirects, canonical metadata, sitemap, robots, social preview, structured data, not-found page, scoped fixes for BD-01, D-02, D-03, D-04, D-12 and the profile route, registry-derived route map, browser and contrast checks, `RELEASE_CANDIDATE.md`.
- Stage 9: PR #80 (`claude/website-stage-09-release-verification`), squash-merged as `05c34be` (the candidate commit); verification record PR #81 (`5862957`).
- Stage 10 executed on branch `claude/website-stage-10-cutover` from `main` at `5862957`. **Blocked after the launch package:** `RELEASE_CANDIDATE.md` §6 row 1 (domain and DNS ownership and executor) arrived unfilled and no existing instruction authorizes a DNS change. No domain, DNS, environment, protection, indexing, migration, or operating-site change was made. The site continues to serve on the `noindex` alias.
- Next action: the product owner records §6 rows 1–3 (domain and DNS, content acceptance, legal review); Stage 10 then resumes at `RELEASE_CANDIDATE.md` §8 "Switch" against the unchanged package. **A release candidate is not launch approval.**
- Website implementation under this plan: **In progress** — Stages 1 to 4 are on `main`; Stage 5 delivers Partners, the investor section, the reconciled team, the newsroom structure, and the three policy pages. Every route in the guide's sitemap now has a live page except the two dynamic templates (`/news/[slug]/`, `/resources/[slug]/`) that wait for an approved record, and `/contact-2/` (Stage 9 redirect).
- Existing website/Command Center functionality: audited in `BASELINE_AUDIT.md`; the unified public site is live on the Vercel production alias, the legacy WordPress site still serves `lifesupplyhealth.com`.
- Production approval, custom-domain cutover, migration application, external storefront edits, and live messaging: **Not performed.**

## Stage register

| Stage | Scope | Status | Evidence / PR | Dependencies or decisions |
| --- | --- | --- | --- | --- |
| 1 | Baseline, source reconciliation, implementation specification | **Ready for Review** (merged to `main` as `fac59a4`, PR #68) | `BASELINE_AUDIT.md`, `SOURCE_REGISTER.md`, `ROUTE_AND_ACTION_MAP.md`, `IMPLEMENTATION_BACKLOG.md`, `evidence/stage-01/` | Business-plan PDFs (S-62) unavailable; Render migration log and env values not readable in session; four external-site admin access unconfirmed. |
| 2 | Shell, Home, About | **Ready for Review** (merged to `main` as `19ca3d5`, PR #69) | `STAGE_02_EVIDENCE.md`, `evidence/stage-02/` | Kickoff decisions arrived unfilled; interim treatments applied (evidence §5). S-120 copy pending approval; WEB-01 relationships `null`; brand cards text-only (WEB-08). BD-02/BD-03 recorded, untouched. |
| 3 | Four brands, Clinic Solutions, Shop & Services | **Ready for Review** (merged to `main` as `fab88e7`, PR #71 superseding #70) | `STAGE_03_EVIDENCE.md`, `evidence/stage-03/`, `OPERATING_SITE_HANDOFFS.md` (draft) | Kickoff decisions arrived unfilled; interim treatments applied (evidence §5): WEB-01 `null` relationships, WEB-03 attribution as the Clinics site states it with no imagery, text-only brand pages. |
| 4 | Metabolic Health and eight pathways | **Ready for Review** (merged to `main` as `622c7a8`, PR #72) | `STAGE_04_EVIDENCE.md`, `evidence/stage-04/` | Kickoff decisions arrived unfilled: WEB-04 status, contents, and refill capability; no store configuration or SKU approved. All pages are in-development information pages with `approvedContents: null`; K03 states it has no destination (S-89). |
| 5 | Partners, investors, leadership, resources | **Ready for Review** (merged to `main` as `2cd81c3`, PR #73) | `STAGE_05_EVIDENCE.md`, `evidence/stage-05/` | Kickoff decisions arrived unfilled: WEB-05 (nothing financing-related named; documents on request), WEB-06 (dated legacy titles; board of six; John Anderson profile only), no approved news or resources (empty, honest). Policy pages state verified behaviour; legal review pending before Stage 7. |
| 6 | Publishing and document delivery | **Ready for Review** (merged to `main` as `de906d7`, PR #74; verification `c065d3d`, PR #75) | `STAGE_06_EVIDENCE.md`, `evidence/stage-06/`, `migrations/stage-06-public-web-governance/` | Decisions arrived unfilled: DEC-03 (no storage; allowlisted-host files only, none configured), roles (existing two permissions, four-eyes approval), no approved documents, WEB-10 (migration prepared, not applied). Dashboard screens not exercised in session (no local database); public endpoints verified on Render after merge; a two-user publish cycle is still owed. |
| 7 | Inquiry capture and handoffs | **Ready for Review** (merged to `main` as `0743a56`, PR #76; verification `ed15745`, PR #77) | `STAGE_07_EVIDENCE.md`, `evidence/stage-07/`, `migrations/stage-07-public-inquiry/` | Decisions arrived unfilled (WEB-10, WEB-07, legal review). Endpoint refuses (flag off, table absent); form unpublished; owners mapped to approved directory channels; retention 180 days default; sending kill-switched with a sink variable. Queue screens not exercised (no local database). |
| 8 | Operating-site integration and measurement | **Ready for Review** (merged to `main` as `68d6af5`, PR #78; verification `1e04402`, PR #79) | `STAGE_08_EVIDENCE.md`, `evidence/stage-08/`, `OPERATING_SITE_HANDOFFS.md` | WEB-08 unfilled: nothing applied externally. No analytics property or consent approval: taxonomy and inert `data-measure` markup only, analytics denied by default. Projection contract recorded, not built. D-12 recorded (Render host sets Auth.js cookies on public routes; Vercel alias sets none). |
| 9 | SEO, accessibility, migration and release verification | **Ready for Review** (merged to `main` as `05c34be`, PR #80; verification `5862957`, PR #81) | `STAGE_09_EVIDENCE.md`, `evidence/stage-09/`, `LEGACY_URL_MAP.md`, `RELEASE_CANDIDATE.md` | Closed: BD-01 (line endings), D-02 (health on the public surface), D-03 (route-family boundary), D-04 (CI Node), D-12 (Auth.js cookies), profile route `force-dynamic`, D-06 (sitemap, robots, structured data, canonical, not-found). Open decisions in `RELEASE_CANDIDATE.md` §6; accepted deferrals proposed in §7. No custom domain or DNS ownership supplied. |
| 10 | Authorized cutover and stabilization | **Blocked** (launch package complete) | `STAGE_10_EVIDENCE.md`, `evidence/stage-10/`, `RELEASE_CANDIDATE.md` §8; branch `claude/website-stage-10-cutover` (PR recorded in the session log) | Gate: `RELEASE_CANDIDATE.md` §6 row 1 (domain and DNS ownership and executor). Also outstanding for an indexable launch: content acceptance (row 2), legal review (row 3). D-13 recorded (preview deployments unprotected and without public mode; owner action in Vercel). Stabilization not started. |

Allowed stage statuses: Not started, In progress, Ready for Review, Blocked, Partially Complete, Accepted. Only record Accepted with actual product-owner/reviewer evidence. Record commit, PR/merge, deployment, and commercial availability separately in the stage evidence. A merge to `main` is recorded beside the status; it is not content acceptance, and the merged pages stay subject to the open decisions listed.

## Decisions to resolve through evidence

These are information needs, not instructions to interrupt the user before doing useful work. Register rows (`S-nn`) are in `SOURCE_REGISTER.md`.

| ID | Decision / verification | Needed for | Evidence gathered in Stage 1 | Safe interim treatment |
| --- | --- | --- | --- | --- |
| WEB-01 | Current entities, brand relationships, contacts and addresses | Public portfolio / contact | Entity-name variants (S-10–S-12), MDEL 13295 on three stores (S-14), Balkowitsch and Clinics relationships unstated (S-15, S-16), two competing office addresses (S-20, S-21) | Use verified facts; omit unsupported legal relationship assertions. |
| WEB-02 | Reconciled financial and customer/product metrics | Home and investors | Seven conflicting counts across legacy and operating sites (S-43–S-51) | Only the three qualified 2025 figures; do not average or combine. |
| WEB-03 | Clinic delivery partners, project attribution, imagery and service geography | Clinics pages | Services, specialties, six BC projects, "core partners" wording, verified consultation and quote URLs (S-70–S-75) | Layout with verified descriptions and approved assets only. |
| WEB-04 | Metabolic program launch status, kit contents/SKUs, compatibility and actual refill capability | Metabolic pages | No kit collection or sharps category exists on any store (S-89, S-90); verified browse URLs for K01/02/04/05/06/07 | Applied in Stage 4: "In development" on every page, `approvedContents: null`, browse links only from the registry, refills page states no service exists; no invented purchasable product. |
| WEB-05 | Current financing/public-market narrative and public vs confidential documents | Investors | 2025 figures approved as qualified (S-60); deck capture date unknown (S-64); 2022 PDF still live on legacy (S-64); $4.2M/CPC/TSXV/CSE/FendX planning-only (S-63) | Applied in Stage 5: status per strand and theme, forward-looking qualification beside each claim, documents index with no hosted file, deck preview unrendered; nothing financing-related named. |
| WEB-06 | Current leadership roster and approved biographies | Team | Title differences between cards and legacy profiles (S-101); John Anderson unresolved (S-102); one portrait held (S-104) | Applied in Stage 5: dated legacy titles on every card, board of six linked to profiles, John Anderson retained as a profile page only. |
| WEB-07 | Inquiry owners, retention, consent, delivery and acknowledgment approval | Stage 7 | Stage 7 built the contract, intake, adapter, deliveries, and queue; owners default to approved directory channels; retention 180 days; consent sentence drafted in the unpublished form; sink variable for acknowledgment tests | Applied in Stage 7: nothing goes live until WEB-07 and WEB-10 are recorded; the contact directory remains the public route. |
| WEB-08 | Admin/repository access and owners for all four external sites | Stage 8 | Platforms identified (WordPress ×2, BigCommerce ×3); no brand asset files held (S-135); Stage 2 brand cards render a mark automatically once `asset` is set on the registry record | Applied in Stage 8: `OPERATING_SITE_HANDOFFS.md` holds exact placements, copy, destinations, UTM strings, prerequisites, and verification steps per site, every row `proposed`; text-only cards remain. |
| WEB-09 | Current deployment/env behavior, baseline build defects and public-host isolation | Stages 1 and 9 | Recorded: D-01–D-12; probes in BASELINE_AUDIT §5 and `evidence/stage-09/local-probes.txt`; Stage 10 added D-13 (Vercel preview deployments have no deployment protection and no `PUBLIC_SITE_MODE`, so crawlers hitting them trigger `MissingSecret` 500s in the proxy's internal branch; production deployments unaffected; no exposure) | Stage 9 closed BD-01, D-02, D-03, D-04, D-06, D-12 with regression tests (`src/proxy.test.ts`, `contrast.test.ts`, browser tests) and no weakened check. Remaining from the baseline: D-05 (contact-2 duplicate, now a redirect), D-07 (pause control, owner decision), D-08 (image-only text, addressed by the rebuild), D-09 (corrected in Stage 6), D-10 (Prisma config deprecation, out of scope), D-11 (auto-deploy on merge, a Stage 10 decision). |
| WEB-10 | Content publication, migration and cutover approvals | Stages 6 and 10 | Publication tables applied on Render (D-09, corrected in docs/36 and docs/37); Stage 6 delivers the editor, approval, publish, withdraw, archive, preview, audit, and published-only readers on the existing schema | Applied in Stage 6: additive migration prepared and not applied; adoption is its own PR after rehearsal (BLK-02) and sign-off. |

## Evidence locations

Stage 1 created:

- `docs/website-development/BASELINE_AUDIT.md`
- `docs/website-development/SOURCE_REGISTER.md`
- `docs/website-development/ROUTE_AND_ACTION_MAP.md`
- `docs/website-development/IMPLEMENTATION_BACKLOG.md`
- `docs/website-development/evidence/stage-01/` — six desktop captures (legacy corporate site, the four operating sites, the unified production alias; 2026-09-08, 1440 px) and `baseline-checks-148617e.txt`

Stage 2 created `docs/website-development/STAGE_02_EVIDENCE.md` and `docs/website-development/evidence/stage-02/` (Home, About, open menus, and footer at 390, 768, and 1440 px; local browser-suite log).

Stage 3 created `docs/website-development/STAGE_03_EVIDENCE.md`, `docs/website-development/evidence/stage-03/` (the twelve Stage 3 pages at 390, 768, and 1440 px; both new menu groups; local browser-suite log), and the draft `docs/website-development/OPERATING_SITE_HANDOFFS.md`.

Stage 4 created `docs/website-development/STAGE_04_EVIDENCE.md` and `docs/website-development/evidence/stage-04/` (the ten Stage 4 pages at 390, 768, and 1440 px; the Metabolic Health menu group at 1440 and the mobile panel at 390 and 768; local browser-suite log).

Stage 5 created `docs/website-development/STAGE_05_EVIDENCE.md` and `docs/website-development/evidence/stage-05/` (eighteen pages at 390, 768, and 1440 px; the Partners and Investors menu groups at 1440; the mobile panel at 390 and 768; local browser-suite log).

Stage 6 created `docs/website-development/STAGE_06_EVIDENCE.md`, `docs/website-development/evidence/stage-06/` (the governed news and documents pages in their fail-closed state and the unpublished-slug 404 at 390, 768, and 1440 px; local browser-suite log; database-free API response), and `docs/website-development/migrations/stage-06-public-web-governance/` (prepared, un-applied migration).

Stage 7 created `docs/website-development/STAGE_07_EVIDENCE.md`, `docs/website-development/evidence/stage-07/` (local intake-route probe log through every refusal path; browser-suite log), and `docs/website-development/migrations/stage-07-public-inquiry/` (prepared, un-applied migration). No public page changed, so no captures were taken.

Stage 8 created `docs/website-development/STAGE_08_EVIDENCE.md` and `docs/website-development/evidence/stage-08/` (the seven pages that gained related links plus the K08 kit page at 390, 768, and 1440 px; browser-suite log; the production alias cookie and script check from a real browser), and completed `docs/website-development/OPERATING_SITE_HANDOFFS.md`.

Stage 10 created `docs/website-development/STAGE_10_EVIDENCE.md` (the launch package and the blocking gate) and `docs/website-development/evidence/stage-10/` (browser suite against the alias; rollback reference and baselines), and recorded the pre-switch state in `RELEASE_CANDIDATE.md` §8.

Stage 9 created `docs/website-development/STAGE_09_EVIDENCE.md`, `docs/website-development/LEGACY_URL_MAP.md`, `docs/website-development/RELEASE_CANDIDATE.md`, and `docs/website-development/evidence/stage-09/` (local defect probes, head tags, sitemap and robots output, provisional lab performance, browser-suite log, and captures of the not-found, home, and contact pages at 320, 375, 390, 768, and 1440 px plus 200% zoom and keyboard focus), and appended the registry-derived tables to `ROUTE_AND_ACTION_MAP.md`.

Later stages create or update `docs/website-development/STAGE_NN_EVIDENCE.md`. Stage 8 maintains `OPERATING_SITE_HANDOFFS.md`. Stage 9 prepares `RELEASE_CANDIDATE.md` referencing the existing cutover runbook. Record screenshots in an appropriate repository evidence folder or durable PR artifact, avoiding confidential content and excessive binary churn.

## Session log

| Date | Stage | Change | Verification | Next action |
| --- | --- | --- | --- | --- |
| 2026-09-08 | Instructions | Comprehensive website guide and one-stage execution workflow prepared. Existing root instructions preserved with a scoped entry point. | Documentation validation recorded in the instruction PR (#67). | Execute Stage 1 only when the product owner supplies the kickoff prompt. |
| 2026-09-08 | 1 | Baseline audit, source register, route/action map, backlog, and evidence produced on `claude/website-stage-01-baseline` from `148617e`. No runtime change. | At `148617e`, Node 24.14.0 / pnpm 10.0.0: `format:check` fail (423 CRLF files, environment; CI green on the same commit), `typecheck` pass, `lint` pass, `test` pass (82 files, 1,092 tests), `public-web:build` pass, `build` pass; Playwright smoke against the production alias: 23 passed, 1 skipped (`evidence/stage-01/public-smoke-production-alias-148617e.txt`). Host-isolation and health probes recorded (D-02, D-03). | PR #68 opened for review (not merged). Product owner reviews Stage 1; resolve order-1 decisions in `IMPLEMENTATION_BACKLOG.md` §16; then issue the Stage 2 prompt. |
| 2026-09-08 | 2 | Brand, route, and action registries; grouped navigation with utility links and a four-brand footer; Home and About page contracts; content model split; canaries and browser tests extended. Branch `claude/website-stage-02-shell-home-about` from `148617e`. | typecheck, lint pass; `pnpm test` 83 files / 1,114 tests pass; public and normal builds pass; local Playwright 31 passed, 3 skipped; repository `format:check` fails only on pre-existing CRLF files (BD-01). CI green on PR #69. Automatic Vercel preview `dpl_CpmiSA8QXWx7LuTpXAkcSasJoTPw` (not a deployment by the stage): Playwright 31 passed, 3 skipped; D-02 and D-03 reproduce there unchanged. | PR #69 open for review (not merged). Product owner reviews Stage 2; supplies S-120, WEB-01, and asset decisions or accepts the interim treatments; merges #67, #68, #69 in that order; then issues the Stage 3 prompt. |
| 2026-09-08 | 3 | Portfolio map, four brand pages, technology and fulfilment, Clinic Solutions hub and three children, Shop & Services, Contact intent routing; registry extensions (verified categories, store links, thirteen actions); nine routes flipped live; draft operating-site handoffs. Branch `claude/website-stage-03-brands-clinics-shop` stacked on the Stage 2 branch (`3993cac`). | typecheck, lint pass; `pnpm test` 83 files / 1,123 tests pass; normal and public builds pass; local Playwright 39 passed, 3 skipped; 44 external URLs verified 200; repository `format:check` fails only on pre-existing CRLF files (BD-01). GitHub Actions did not run on PR #70 because `ci.yml` triggers only on pull requests to `main`. Automatic Vercel preview `dpl_7ow7EVh8BQJap81VLqJHwAAjMq8C` (not a deployment by the stage): Playwright 39 passed, 3 skipped; every Stage 3 route 200; D-02 and D-03 reproduce unchanged. | PR #70 open for review (not merged). Product owner reviews Stage 3; supplies WEB-01, WEB-03, and asset decisions or accepts the interim treatments; merges #67, #68, #69, then the Stage 3 PR; then issues the Stage 4 prompt. |
| 2026-09-08 | Merges | On the product owner's instruction to commit and squash-merge after each stage: #67 → `a355cf6`, #68 → `fac59a4`, #69 → `19ca3d5`. #70 was auto-closed by GitHub when its base branch was deleted, so its commits were rebased onto `main` and merged as #71 → `fab88e7`. STATUS add/add conflicts resolved by keeping each stage branch's newer copy. | CI green on #71 (typecheck/lint/format/tests, production build, Vercel). The production alias served `/clinic-solutions`, `/our-operations/lifesupply-clinics`, `/shop`, `/contact` with 200 within about a minute of the Stage 3 merge (`dpl_Gp3QYn9Yraq4RQNZwrdnRCkht26g`). | Cut Stage 4 from `fab88e7`. |
| 2026-09-08 | 4 | Metabolic Health hub, care-kit hub, eight kit pages (K01–K08), and Refills as in-development information pages; `KitPathway` content model with typed starter/consumable/occasional roles, compatibility, exclusions, registry-resolved browse links; `getBrandCategory`; three routes flipped live plus eight kit routes; three navigation actions; five canaries, registry tests, three browser tests. Branch `claude/website-stage-04-metabolic-health` from `fab88e7`. | typecheck, lint pass; `pnpm test` 83 files / 1,130 tests pass; public and normal builds pass (eight kit pages pre-rendered, unknown slug 404); local Playwright 45 passed, 3 skipped (`evidence/stage-04/playwright-public.txt`); changed files pass Prettier; repository `format:check` fails only on pre-existing CRLF files (BD-01). | PR #72 opened, CI green, squash-merged as `622c7a8` on the standing instruction; the production alias served every Stage 4 route within two minutes. Product owner supplies WEB-04 (status, contents, refill capability) or accepts the in-development treatment. |
| 2026-09-08 | 5 | Partners hub and four children; investor hub rebuilt with growth strategy, advanced therapeutics, documents index, shareholder services, disclosures; team reconciled to dated legacy titles with a linked board of six; newsroom restructured (current, historical, resources) with the two dynamic templates; privacy, terms, accessibility; homepage cross-links to the Metabolic Health and Partners hubs; footer legal row; thirteen routes flipped live; five actions; seven canaries, registry tests, four browser tests. Branch `claude/website-stage-05-partners-investors` from `622c7a8`. | typecheck, lint pass; `pnpm test` 83 files / 1,140 tests pass; public and normal builds pass (45 pages); local Playwright 53 passed, 3 skipped (`evidence/stage-05/playwright-public.txt`); changed files pass Prettier; repository `format:check` fails only on pre-existing CRLF files (BD-01). | PR #73 opened, CI green, squash-merged as `2cd81c3` on the standing instruction; the production alias served every Stage 5 route within two minutes. Product owner supplies WEB-05, WEB-06, and any approved news or resources, or accepts the interim treatments; arranges legal review of the policy pages before Stage 7. |
| 2026-09-08 | 6 | Publication workflow on the existing schema (families and validation, permission-checked service with four-eyes approval, optimistic concurrency, eight audit events), published-only family readers and six public API routes with a generic 503, dashboard editor/reviewer/document screens and a permission-gated noindex preview, database-free public client with fail-closed sections, news and resources moved to the governed model, published-documents section, additive migration prepared and not applied, docs/36 and docs/37 corrected (D-09). Branch `claude/website-stage-06-governed-publishing` from `2cd81c3`. | typecheck, lint pass; `pnpm test` 88 files / 1,185 tests pass (42 new server-side tests: denial, published-only projection, expiry and withdrawal, conflicts, generic 503, guessed-id 404, client fail-closed); public and normal builds pass; local Playwright 57 passed, 3 skipped (`evidence/stage-06/playwright-public.txt`); local API without a database answers the generic 503; changed files pass Prettier; repository `format:check` fails only on pre-existing CRLF files (BD-01). Dashboard screens not exercised (no local database). | PR #74 opened, CI green, squash-merged as `de906d7` on the standing instruction. Post-merge verification (2026-09-08, UTC): Render served `/api/public/v1/news`, `/resources`, `/documents` with 200 and empty lists about three minutes after the merge; unknown slug and guessed document id answered 404; `Cache-Control: public, s-maxage=300, stale-while-revalidate=900`; `/public-web-preview/anything` without a session redirected to `/login`. The production alias built before Render finished, showed the fail-closed note, and revalidated to the honest empty note within about five minutes, as designed; the Vercel `/api/public/v1/news` answers the generic 503 (no database). **Not yet exercised:** a publish cycle with two users on Render; the product owner or an approver should run create → submit → approve (second user) → publish → withdraw once and confirm the audit history. Product owner supplies DEC-03, WEB-10, and any approved public documents; then issues the Stage 7 prompt. |
| 2026-09-08 | 7 | Inquiry contract (eleven intents, strict validation, forbidden recipient and redirect keys, server-side owner mapping, retention, salted client hash, redaction), abuse controls (origin allowlist, 16 KB ceiling, in-process rate limit), persistence adapter with a raw-SQL store that refuses until the prepared table exists, intake endpoint (201/200/413/403/400/503/429, no echo, no-store), two-state deliveries behind a kill-switched flag with a sink, permission-gated staff queue with assignment, status, and Task hand-off, unpublished public form, two feature flags, prepared `public_inquiries` migration. Branch `claude/website-stage-07-inquiry-capture` from `c065d3d`. | typecheck, lint pass; `pnpm test` 94 files / 1,231 tests pass (46 new: single persistence, duplicate and retry safety, invalid and abusive refusal, no success on failed storage, recipient injection, unauthenticated queue denial, deliveries, route mapping); public and normal builds pass; local Playwright 57 passed, 3 skipped; local intake probes without a database answer 405/403/400/503/413 with no visitor data in the log (`evidence/stage-07/intake-route-local.txt`); changed files pass Prettier; repository `format:check` fails only on pre-existing CRLF files (BD-01). Queue screens not exercised (no local database). | PR #76 opened, CI green, squash-merged as `0743a56` on the standing instruction. Post-merge verification (2026-09-08, UTC): about three minutes after the merge Render answered `GET /api/public/v1/inquiries` 405, `POST` without an Origin 403, `POST` with the alias Origin 403 (no `PUBLIC_SITE_ORIGINS` configured), `/public-web/inquiries` without a session 302 to login; the production alias `/contact` still renders the directory with no form. Product owner records WEB-07 and WEB-10, adopts both prepared migrations after rehearsal, then publishes the form; issues the Stage 8 prompt. |
| 2026-09-08 | 8 | `OPERATING_SITE_HANDOFFS.md` completed per site (placements, exact copy, destinations, UTM strings, owners unknown, prerequisites, verification; all `proposed`); measurement taxonomy with six events, allowlisted parameters, consent denied by default, inert `data-measure` markup on store, consultation, quote, investor-materials, and document links, server-confirmed `inquiry_submitted` after persistence; contextual related links on seven pages through two new registry actions; product-projection contract with stale rule. Branch `claude/website-stage-08-operating-site-integration` from `ed15745`. | typecheck, lint pass; `pnpm test` 97 files / 1,243 tests pass; public and normal builds pass; local Playwright 61 passed, 3 skipped (`evidence/stage-08/playwright-public.txt`); production alias in a real browser: no cookie, no third-party script (`evidence/stage-08/alias-cookies-and-scripts.txt`); changed files pass Prettier; repository `format:check` fails only on pre-existing CRLF files (BD-01). D-12 recorded. | PR #78 opened, CI green, squash-merged on the standing instruction (merge commit in the Stage 8 handoff). Post-merge verification (2026-09-08, UTC): the production alias served the related-link rows on the metabolic hub and the LifeSupply brand page and the `data-measure` attributes on the homepage within minutes, and a GET set no cookie. Product owner supplies WEB-08 owners and access, the analytics property and consent approval, and any projection decision; then issues the Stage 9 prompt. |
| 2026-09-09 | 9 | Legacy URL map (21 pages kept or redirected; feeds and WordPress endpoints archived); `/contact-2` and `/ross-jelveh` permanent redirects; `publicMetadata()` on every public route (canonical unslashed, Open Graph, Twitter, robots), JSON-LD on the homepage, `sitemap.ts` (54 URLs), `robots.ts` (disallow all until `PUBLIC_SITE_INDEXABLE=true`), `og-default.jpg` from the hero poster, host-aware `not-found.tsx`; scoped fixes: `.gitattributes` (BD-01), public-surface health (D-02), public-host allowlist decided before the Auth.js wrapper (D-03, D-12), CI Node 24 (D-04), static profile route; surface token one step lighter for AA contrast; registry-derived route map; `RELEASE_CANDIDATE.md`. Branch `claude/website-stage-09-release-verification` from `1e04402`. | **`pnpm format:check` passes on the whole repository**; typecheck, lint pass; `pnpm test` 99 files / 1,256 tests pass (proxy and contrast tests new); public and normal builds pass; local Playwright 68 passed, 4 skipped; local probes: health 200 `public-web`, seven internal families 307 to `/`, unknown path 404, zero `Set-Cookie`, redirects 308, sitemap 54 canonical URLs, robots disallow, canonical and noindex meta present (`evidence/stage-09/`). Lab performance provisional (LCP 168–1,488 ms local, CLS 0). External accessibility audit not performed. | PR #80 opened, CI green on Node 24, squash-merged as `05c34be` on the standing instruction. **Candidate commit: `05c34be`.** Post-merge verification (2026-09-09, UTC, `evidence/stage-09/post-merge-verification.txt`): alias `/api/health` 200 `surface: public-web` (D-02 closed on the alias); `/contact-2` 308 to `/contact`; `/sitemap.xml` 200 with 54 URLs; `/robots.txt` `Disallow: /`; `/no-such-page` 404; `/customers` 307 to `/`; zero `Set-Cookie`; canonical and `noindex` meta present. Render `/api/health` 200 with the full checks; `/api/public/v1/news` 200; `/dashboard` without a session 302 to login. Product owner records the `RELEASE_CANDIDATE.md` §6 decisions and accepts or reopens the §7 deferrals; then issues the Stage 10 prompt. |
| 2026-09-09 | 10 | Launch package assembled against candidate `05c34be` (gates re-run, browser suite against the alias, Vercel project and deployment state, deployment protection, 7-day runtime errors, legacy host and DNS, Render baselines, owners, rollback targets). No cutover action taken. D-13 recorded. Branch `claude/website-stage-10-cutover` from `5862957`. | format:check, typecheck, lint pass; `pnpm test` 99 files / 1,256 tests pass; public and normal builds pass; Playwright against the alias 68 passed, 4 skipped (`evidence/stage-10/playwright-alias.txt`); alias health 200 `public-web`; Render health 200 all checks ok; legacy host 200 at `165.232.130.127`; no runtime error on a production deployment in the last hour (`evidence/stage-10/rollback-reference-and-baselines.txt`). | **Blocked** on `RELEASE_CANDIDATE.md` §6 row 1 (domain and DNS ownership and executor). Stage 10 PR (documentation) opened and squash-merged per the standing instruction; exact PR number and merge commit in the handoff. Product owner supplies rows 1–3 and, in Vercel, protects preview deployments or sets `PUBLIC_SITE_MODE=true` on Preview (D-13); then re-issues the Stage 10 prompt to resume at the switch. |
| 2026-09-08 | Design pass | Site-wide layout, graphics, icon, and interaction pass at the product owner's request, before content fine-tuning: seven section primitives and a FAQ accordion (21st.dev adaptations on the brand tokens), an icon registry and title map, nine conceptual Gamma graphics with provenance (S-136 to S-144), every page family re-laid-out with its own rhythm, repetition removed; two reduced-motion defects in the motion primitives fixed (hydration text mismatch on figures; server-rendered hidden styles never cleared). No copy, route, action, registry, or backend change. Branch `claude/website-design-pass` from `e098a03`. | format:check, typecheck, lint pass; `pnpm test` 101 files / 1,262 tests pass; public build passes; local Playwright 68 passed, 4 skipped (`evidence/design-pass/playwright-public.txt`); 90 captures at 390/768/1440 under reduced motion with no console error, overflow, or hidden section (`evidence/design-pass/`). | PR opened, CI, squash-merge on the standing instruction; product owner reviews the graphics and begins page-by-page content changes. Domain, DNS, SEO, and indexing still deferred. |
| 2026-09-08 | Design pass follow-up | Mobile panel groups collapse behind an expand button (one open at a time, the current page's group open first) so the menu fits a phone screen; LLD-style back-to-top control bottom right, rendered only after scrolling, focus returned to main, reduced motion honoured. Branch `claude/website-mobile-menu-scroll-top` from `22632cb`. | format:check, typecheck, lint pass; public-site tests 122 pass; public build passes; Playwright navigation, panel, and back-to-top tests pass; captures in `evidence/design-pass/` (`DESIGN_PASS_EVIDENCE.md` §7). | PR, CI, squash-merge on the standing instruction. |
| 2026-09-08 | Content decision | On the product owner's instruction: the homepage hero drops the "Plan a clinic" action (Clinic Solutions keeps it); MedDirect Distribution Corporation and Dexton Medical Corporation are no longer operational, so the two entities, the showroom channel at a Dexton address, the portfolio lockup, and the legacy timeline graphic are removed from every public page and from `public/lsh/` (files kept in `docs/website-development/legacy-assets/`). A canary now rejects any mention. Branch `claude/website-hero-and-entities` from `40beb8d`. | format:check, typecheck, lint, tests, public build, browser suite recorded in the PR. | PR, CI, squash-merge on the standing instruction. WEB-01 entity question resolved (S-13). |
| 2026-09-08 | Brand photographs (image handoff Stage 2) | Asset PR #86 merged first; the four product-owner greyscale brand photographs integrated: JPEG derivatives under the byte budget, four registry entries with their own provenance and crop positions, a shared `BrandImage` component, square photographs on the Home brand grid, Shop & Services, and Our Businesses, and the brand pages' wide sections switched to their own photograph (S-145 to S-148). Branch `claude/website-brand-photos` from `f5c6d44`. | format:check, typecheck, lint, tests, public build, browser suite, and captures at 390/768/1440 recorded in `BRAND_GREYSCALE_STAGE_2.md` and `evidence/brand-photos/`. | PR, CI, squash-merge on the standing instruction; product owner reviews the square crops. |
| 2026-09-08 | Content decision | Homepage: the conceptual exam-room image beside the clinic lifecycle and the supplies image beside the metabolic section removed on the product owner's instruction; the lifecycle becomes three full-width step columns and the metabolic section a two-column statement. No copy change. Branch `claude/website-home-images` from `4101bcb`. | format:check, typecheck, lint, public-site tests, public build, browser suite 71 passed / 5 skipped; captures `evidence/design-pass/home-no-section-images-{390,1440}.jpg`. | PR, CI, squash-merge on the standing instruction. |
| 2026-09-08 | Content decision | Technology & fulfilment removed on the product owner's instruction (not a major business unit): page, route, navigation entry, action, content block, and the Our Businesses callout withdrawn; the address permanently redirects to `/our-operations`; the investor growth strand for existing-business improvement now points at the Our Businesses hub. Branch `claude/website-remove-technology` from `e57f110`. | format:check, typecheck, lint, tests, public build, browser suite recorded in the PR. | PR, CI, squash-merge on the standing instruction. |
| 2026-09-08 | Restructure | On the product owner's direction: Our Businesses → Medical Supply Solutions (`/medical-supply-solutions/`, the three stores; corporate portfolio moved to About); Clinic Solutions is now the LifeSupply Clinics section with the plan/equip/keep-supplied spine (brand page and Design & build merged in; Equipment and Ongoing supplies as children); Pharmacy Solutions added as an information section with its status; Metabolic Health hub rebuilt from the September 2026 partner overview (connected experience, four value streams, care partners, configuration dimensions, next step, important information) with the in-development status kept; team page reduced to Abdul Ladha with the board and eleven profiles withdrawn; the online-sales channel withdrawn and supply reviews routed to the corporate office; every withdrawn address permanently redirected. Branch `claude/website-restructure` from `0ffa083`. | format:check, typecheck, lint, tests (101 files / 1,267), public build, browser suite, captures at 390/768/1280/1440 recorded in `RESTRUCTURE_2026-09-08_EVIDENCE.md`. | PR, CI, squash-merge on the standing instruction. Product owner reviews the Pharmacy Solutions wording and the Metabolic Health hub. |
| 2026-09-09 | Content and navigation | On the product owner's instruction: the historical company news block removed from the homepage (the newsroom keeps it); "Home" added as the first primary menu item; the mobile panel's vertical spacing tightened; each Medical Supply Solutions store page shows its live home page on a laptop frame in the "On this site" block (dated captures of lifesupply.ca, wellmartmedical.com, and balkowitsch.com at 1440×900 with promotional overlays hidden, composed at 1600×1000; registry `site-screens.ts` with its own test). Branch `claude/website-home-menu-mockups` from `62981ae`. | format:check, typecheck, lint, tests (102 files / 1,270), public build, browser suite, header probe at 1280 and 1366, captures recorded in `evidence/home-menu-2026-09-09/`. | PR, CI, squash-merge on the standing instruction. Refresh the store screens when a storefront changes. |
| 2026-09-09 | Content | Clinic Solutions: the lifesupplyclinics.com home page on a laptop frame beside "As presented by LifeSupply Clinics" (dated capture, S-152), matching the store pages. Branch `claude/website-clinics-screen` from `31e1ac4`. | format:check, typecheck, lint, tests, public build, browser suite, capture at 390 and 1440. | PR, CI, squash-merge on the standing instruction. |
| 2026-09-09 | Presentation decision | Visible image notices removed on the product owner's instruction: no "Conceptual image" captions on the design-pass graphics and brand photographs, and no capture-date notice on the store screens, now or going forward. Alt text and the registries keep the provenance and dates for the record; a canary now rejects any visible caption in the public components. Same branch as the Clinics screen (`claude/website-clinics-screen`). | Gates recorded in the PR. | Product owner: the guide §4 convention of labelling conceptual imagery on the page is superseded by this decision; provenance stays in `SOURCE_REGISTER.md`. |
| 2026-09-09 | Navigation | Every desktop dropdown and every expanded mobile group now opens with an "Overview" row to the group's own page, so a visitor who goes straight to the dropdown does not miss the hub (product owner's question, 2026-09-09). The overview row never claims aria-current; the trigger stays the one current link. Branch `claude/website-menu-overview` from `dd9b86c`. | format:check, typecheck, lint, tests, public build, browser suite; captures of the open dropdown and an expanded mobile group in `evidence/home-menu-2026-09-09/`. | PR, CI, squash-merge on the standing instruction. |
| 2026-09-09 | Wording | Advanced therapeutics: the four items are "options under evaluation", not "themes" (product owner); the investor hub card, the pharmacy cross-reference, and the route description follow. Branch `claude/website-therapeutics-wording` from `05503b0`. | Gates in the PR. | PR, CI, squash-merge on the standing instruction. |
| 2026-09-09 | Design | Homepage counters enlarged: the three reported figures are now full-width cards under the heading with 8xl condensed numerals, the sign or unit in brand red, a red rule that extends on hover, and a faint red corner glow; the investor pages' figures share the style at the default size. No figure or label changed. Branch `claude/website-home-counters` from `e2956a5`. | Gates and browser suite in the PR; captures `evidence/home-menu-2026-09-09/counters-*.jpg`. | PR, CI, squash-merge on the standing instruction. |
| 2026-09-09 | Design | Footer: the Operating brands and Explore lists' vertical spacing reduced (product owner). Branch `claude/website-footer-spacing` from `7c083ae`. | canaries, build, browser suite 71 passed / 5 skipped. | PR, CI, squash-merge on the standing instruction. |
| 2026-09-09 | Navigation | Pharmacy Solutions moved after Clinic Solutions and before Metabolic Health in every menu (header, mobile panel, footer Explore) on the product owner's instruction; the route table carries the order so the sitemap follows; the divider under the Overview row removed. Branch `claude/website-menu-order` from `a52efe6`. | canaries, build, browser suite in the PR. | PR, CI, squash-merge on the standing instruction. |
